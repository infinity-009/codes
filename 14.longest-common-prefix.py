#
# @lc app=leetcode id=14 lang=python3
#
# [14] Longest Common Prefix
#

# @lc code=start
class Solution:
    def longestCommonPrefix(self, strs: List[str]) -> str:
        # m=len(strs[0])   # simple approach O(sum(lengths))
        # ans=""
        # for i in range(m):
        #     temp=strs[0][i]
        #     for j in range(len(strs)):
        #         m=min(m,len(strs[j]))
        #         if(i>=m or temp!=strs[j][i]) : return ans
        #     ans+=temp
        # return ans

        

# @lc code=end

